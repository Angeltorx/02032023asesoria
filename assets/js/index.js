//console.log("Entro index.js");

const lista=[]

//create
const btnadd = (algo) => {
    lista.push(algo);
}

// read
const btnread=()=>{
    lista.btnread
}

//update
const  btnedit= (position, newValue) => {
    lista.slice(position, 1, newValue);
}

//* delete
const btndelete= (position)=>{
    lista.splice(position, 1)
}

